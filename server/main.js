import {Meteor} from 'meteor/meteor';
import {FixedData} from './../imports/api/fixedData'
import {AddData} from './../imports/ui/AddData'

 Meteor.startup(() => {
    // Meteor.methods({
    //     'saveFile': function(buffer){
    //         FixedData.insert({uploadedfile: buffer})
    //     }
    // })
})